# AI Architect Mastery — robots.txt
# Note: aiarchitectmastery.com domain is registered at Namecheap but
# DNS is NOT yet pointed at GitHub Pages. Crawling is currently allowed —
# canonical URLs point to the production domain so any github.io discovery
# will be deduplicated by search engines once DNS is live.
# To block all indexing pre-launch, change `Disallow:` to `Disallow: /`.

User-agent: *
Disallow:

Sitemap: https://aiarchitectmastery.com/sitemap.xml

# AI / LLM crawler guidance (llmstxt.org standard)
# https://aiarchitectmastery.com/llms.txt
